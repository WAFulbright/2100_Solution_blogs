
# export PATH="/opt/local/bin:/opt/local/sbin:/Users/williamfulbright/bin:/User$
# export PATH="/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin:/opt$
# export PATH="/usr/local/bin:$PATH"
# export PATH="/Users/williamfulbright/.rvm/gems/ruby-2.2.4/bin"
# # export PATH="/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin$PATH"
# source ~/.profile
# if [ -f ~/.bashrc ]; then
#    source ~/.bashrc
# fi
##
# Your previous /Users/williamfulbright/.bash_profile file was backed up as /Us$
##
# MacPorts Installer addition on 2015-11-06_at_02:49:01: adding an appropriate $
# export PATH="/opt/local/bin:/opt/local/sbin:"
# Finished adapting your PATH environment variable for use with MacPorts.

# Load RVM into a shell session *as a function*
# export PATH="$HOME/.rbenv/bin:$PATH"

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM$


[[ -s "$HOME/.profile" ]] && source "$HOME/.profile" # Load the default .profile

source $HOME/.bashrc

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM$

export JAVA_HOME="$(/usr/libexec/java_home)"

# Setting PATH for Python 3.5
# The original version is saved in .bash_profile.pysave
PATH="/Library/Frameworks/Python.framework/Versions/3.5/bin:${PATH}"
export PATH

# Setting PATH for Python 3.5
# The original version is saved in .bash_profile.pysave
PATH="/Library/Frameworks/Python.framework/Versions/3.5/bin:${PATH}"
export PATH
